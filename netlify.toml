[build]
  command = "npm run build"
  publish = ".next"

# Disable secrets scanning for known safe values
[build.environment]
  SECRETS_SCAN_ENABLED = "false"

[[plugins]]
  package = "@netlify/plugin-nextjs"
